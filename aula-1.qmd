---
title: "Estatistica para ciencia de dados"
format: html
editor: visual
---

# Carregando os pacotes

```{r}
library(tidyverse)
library(readr)
library(skimr)
library(gt)
```

```{r}
install.packages("conflicted")
library(conflicted)
library(tidyverse)
conflicts_prefer(dplyr::filter)
```
# Dados 

### Carregando dados

```{r}
df <- read_csv("data/Aula-1-churn_clean.csv") 
View(df)
```

### Dando uma olhada geral nos dados

```{r}
glimpse(df)
```
